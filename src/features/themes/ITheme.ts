export interface ITheme {
    body: string
    text: string
    toggleBorder: string
    background: string,
    primary: string,
    primaryActive: string,
    primaryHighlight: string
}

export default ITheme